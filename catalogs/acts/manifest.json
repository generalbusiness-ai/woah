{
  "name": "acts",
  "version": "0.1.0",
  "spec_version": "v1",
  "description": "Acts kernel (notes/2026-07-21-acts-projection-model.md): the generic substrate-adjacent layer only. The $acts feature gives any $space subclass an internal, schema-validated, fail-closed act-emission primitive on its own sequenced log; $projection is the base for single-writer folds with bounded views and incremental idempotent rebuild from recorded observations. Domain classes, schemas, and concrete projections live in consuming catalogs (see casework for the proof; the tasks migration is the first real consumer).",
  "license": "MIT",
  "depends": [],
  "features": [
    {
      "local_name": "$acts",
      "parent": "$thing",
      "description": "Stateless feature carrying the internal act-emission primitive. Mounted on a $case (or any $space subclass that adopts the kernel), its effective surface is <space>:act. Emission requires a sequenced turn on the consumer's own log and an internal caller; payloads validate against event_schema; attached projections fold in the same turn, fail-closed.",
      "properties": [],
      "verbs": [
        {
          "name": "can_be_attached_by",
          "perms": "rxd",
          "arg_spec": {
            "args": [
              "who"
            ]
          },
          "source": "verb :can_be_attached_by(who) rxd {\n  /* Mounting the acts feature is a seed/owner act on the consumer side\n     ($case:initialize); the feature itself is freely attachable. */\n  return true;\n}",
          "direct_callable": true
        },
        {
          "name": "act",
          "perms": "rx",
          "arg_spec": {
            "args": [
              "type",
              "payload"
            ]
          },
          "source": "verb :act(type, payload) rx {\n  /* Internal emission primitive (acts kernel S2.4). Requires a sequenced\n     turn on this room's own log (direct routes carry seq == -1), called\n     from this room's own verbs (caller == this). Validates the payload\n     against the declared event schema, folds every attached projection\n     inside the same turn, then records the act observation. Any fold\n     failure aborts the whole turn: fail-closed. */\n  if (seq < 1 || space != this) { raise { code: \"E_INVARG\", message: \"acts require a sequenced turn on this room\" }; }\n  if (caller != this) { raise { code: \"E_PERM\", message: \"acts are emitted by room verbs only\" }; }\n  let shape = event_schema(this, type);\n  if (shape == null) { raise { code: \"E_INVARG\", message: \"unknown act type\", value: type }; }\n  this:_validate_payload(type, shape, payload);\n  let act = { \"type\": type, \"version\": 1, \"payload\": payload };\n  /* Folds receive the act plus its envelope seq, injected here (live) or\n     by :rebuild_from (recorded). The observed act body stays free of\n     envelope fields \u2014 the log entry is the envelope. */\n  let fold_input = { \"type\": type, \"version\": 1, \"payload\": payload, \"seq\": seq };\n  for p in this.projections {\n    if (valid(p) && (type in p.consumes)) { p:fold(fold_input); }\n  }\n  observe(act);\n  return act;\n}",
          "skip_presence_check": true
        },
        {
          "name": "_validate_payload",
          "perms": "rx",
          "arg_spec": {
            "args": [
              "type",
              "shape",
              "payload"
            ]
          },
          "source": "verb :_validate_payload(type, shape, payload) rx {\n  /* Closed flat shape subset (S2.2): obj, str, bool, int, float, num,\n     list, map. Every declared key is required; undeclared keys refuse.\n     Exact typing: a \"str\" field refuses live object refs (refs are\n     strings at the VM level - an obj must not pass as a code/label),\n     and \"obj\" requires a live ref. */\n  if (typeof(payload) != \"map\") { raise { code: \"E_INVARG\", message: \"act payload must be a map\", value: type }; }\n  for pk, pv in payload {\n    if (!(pk in shape)) { raise { code: \"E_INVARG\", message: \"undeclared act payload key\", value: pk }; }\n  }\n  for k, t in shape {\n    if (!(k in payload)) { raise { code: \"E_INVARG\", message: \"missing act payload key\", value: k }; }\n    let val = payload[k];\n    let ok = false;\n    if (t == \"obj\") { ok = typeof(val) == \"string\" && valid(val); }\n    else if (t == \"str\") { ok = typeof(val) == \"string\" && !valid(val); }\n    else if (t == \"bool\") { ok = typeof(val) == \"boolean\"; }\n    else if (t == \"int\") { ok = typeof(val) == \"number\" && floor(val) == val; }\n    else if (t == \"float\" || t == \"num\") { ok = typeof(val) == \"number\"; }\n    else if (t == \"list\") { ok = typeof(val) == \"list\"; }\n    else if (t == \"map\") { ok = typeof(val) == \"map\"; }\n    else { raise { code: \"E_INVARG\", message: \"unsupported shape tag\", value: t }; }\n    if (!ok) { raise { code: \"E_INVARG\", message: \"act payload type mismatch\", value: k }; }\n  }\n  return true;\n}",
          "skip_presence_check": true
        }
      ]
    }
  ],
  "classes": [
    {
      "local_name": "$projection",
      "parent": "$thing",
      "flags": {
        "fertile": true
      },
      "description": "Base class for act projections (acts kernel S2.3): consumes + fold + rows + view. rows/consumes/row_cap/at_seq are perms-empty so only catalog-author verbs write them \u2014 the fold is the sole writer of coordination rows. A projection lives in its room's anchor cluster so fold writes share the turn's atomicity.",
      "properties": [
        {
          "name": "rows",
          "type": "map",
          "default": {},
          "perms": ""
        },
        {
          "name": "consumes",
          "type": "list",
          "default": [],
          "perms": ""
        },
        {
          "name": "row_cap",
          "type": "int",
          "default": 1000,
          "perms": ""
        },
        {
          "name": "at_seq",
          "type": "int",
          "default": 0,
          "perms": ""
        },
        {
          "name": "rebuild_scan_seq",
          "type": "int",
          "default": 0,
          "perms": ""
        }
      ],
      "verbs": [
        {
          "name": "fold",
          "perms": "rx",
          "arg_spec": {
            "args": [
              "act"
            ]
          },
          "source": "verb :fold(act) rx {\n  /* Subclass responsibility (S2.3): deterministic given (rows, act), the\n     sole writer of this.rows, O(payload), no clock, no foreign reads.\n     The act input carries the envelope seq as act[\"seq\"]. The base\n     refuses so an unimplemented projection fails loudly. */\n  raise { code: \"E_VERBNF\", message: \"projection must implement :fold\" };\n}",
          "skip_presence_check": true
        },
        {
          "name": "view",
          "perms": "rxd",
          "arg_spec": {
            "args": [
              "opts"
            ]
          },
          "source": "verb :view(opts) rxd {\n  /* The one authoritative bounded read (S2.3). Pages rows in key order\n     with a continuation cursor: pass opts[\"after\"] = the previous\n     page's \"cursor\" to fetch the next page (a stale/unknown cursor\n     yields an empty page). at_seq is the seq of the LAST CONSUMED act -\n     a conservative completeness watermark, not the room's head seq.\n     Each row renders through :view_row so subclasses join\n     object-authoritative facts at view time without mirroring them. */\n  if (typeof(opts) != \"map\") { opts = {}; }\n  let limit = 50;\n  if (\"limit\" in opts) { limit = toint(opts[\"limit\"]); }\n  if (limit < 1 || limit > 200) { limit = 50; }\n  let after = \"\";\n  if (\"after\" in opts) { after = to_string(opts[\"after\"]); }\n  let skipping = after != \"\";\n  let page = [];\n  let cursor = null;\n  let has_more = false;\n  for k in keys(this.rows) {\n    if (skipping) { if (k == after) { skipping = false; } continue; }\n    if (length(page) >= limit) { has_more = true; break; }\n    page = page + [this:view_row(k, this.rows[k])];\n    cursor = k;\n  }\n  return { \"page\": page, \"at_seq\": this.at_seq, \"cursor\": cursor, \"has_more\": has_more };\n}",
          "direct_callable": true,
          "skip_presence_check": true,
          "tool_exposed": true
        },
        {
          "name": "view_row",
          "perms": "rx",
          "arg_spec": {
            "args": [
              "key",
              "row"
            ]
          },
          "source": "verb :view_row(key, row) rx {\n  /* Default rendering: the stored row itself. */\n  return row;\n}",
          "skip_presence_check": true
        },
        {
          "name": "rebuild_from",
          "perms": "rxd",
          "arg_spec": {
            "args": [
              "source_space",
              "from_seq"
            ]
          },
          "source": "verb :rebuild_from(source_space, page_budget) rxd {\n  /* Operator rebuild (gate 1): re-fold recorded acts from the source\n     log. Incremental and idempotent: resumes after the furthest of\n     at_seq (live folds) and rebuild_scan_seq (prior rebuild scans), so\n     repeated calls never double-fold. Bounded: processes at most one\n     replay page per call - callers loop while done == false. A\n     reset/reseed path belongs with genesis replacement, not here.\n     Owner/wizard only. Skips failed entries. */\n  if (actor != this.owner && !has_flag(actor, \"wizard\")) { raise { code: \"E_PERM\", message: \"rebuild is owner-only\" }; }\n  let budget = 100;\n  if (typeof(page_budget) == \"number\" && page_budget >= 1 && page_budget <= 100) { budget = toint(page_budget); }\n  let from = this.at_seq;\n  if (this.rebuild_scan_seq > from) { from = this.rebuild_scan_seq; }\n  from = from + 1;\n  let entries = source_space:replay(from, budget);\n  let scanned = from - 1;\n  for e in entries {\n    scanned = e[\"seq\"];\n    if (!e[\"applied_ok\"]) { continue; }\n    for o in e[\"observations\"] {\n      if (typeof(o) == \"map\" && (\"type\" in o) && (\"payload\" in o) && (\"version\" in o) && (o[\"type\"] in this.consumes)) {\n        let input = o;\n        input[\"seq\"] = e[\"seq\"];\n        this:fold(input);\n      }\n    }\n  }\n  this.rebuild_scan_seq = scanned;\n  return { \"at_seq\": this.at_seq, \"scanned_seq\": scanned, \"done\": length(entries) < budget };\n}",
          "direct_callable": true,
          "skip_presence_check": true
        }
      ]
    }
  ]
}
