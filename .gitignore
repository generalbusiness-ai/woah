node_modules
dist/
coverage/
.DS_Store
.env
.env.*
.dev.vars
.mcp.json
.woo/
# Local workerd state created by `wrangler dev` (smoke:cf-dev lane).
.wrangler/
.claude/
.clawpatch/
test-results/
playwright-report/
playwright/.cache/
# Cutover exports contain password hashes, API-key verifiers, and account
# identifiers. Keep the default and recommended artifact locations untracked.
identity-export*.json
cutover-artifacts/
node_modules
